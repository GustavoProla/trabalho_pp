import React from 'react';

const Damn = () => {
  const damnStyle = {
    width: '100vw',
    height: '100vh',
    display: 'flex',
    justifyContent: 'center',
    backgroundColor: '#f2f2f2',
  };

  return <div style={damnStyle}>Conteúdo no meio da tela</div>;
};

export default Damn;